PYTHON:
name = [4,3,2,1]
name.sort()
---------------------------------------------
JAVA:
import java.util.Arrays;
type[] name = new type[] {4,3,2,1};
Arrays.sort(name);
---------------------------------------------
CPP:
#include <algorithm>
#include <vector>
std::vector<int> name = {4,3,2,1};
name.sort(name.begin,name.end);